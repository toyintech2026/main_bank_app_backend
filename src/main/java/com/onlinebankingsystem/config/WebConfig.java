package com.onlinebankingsystem.config;

public class WebConfig {
    // CORS is now handled inside SecurityConfig.java.
    // This file is intentionally left without any active Spring configuration
    // to avoid duplicate corsFilter bean conflicts.
}